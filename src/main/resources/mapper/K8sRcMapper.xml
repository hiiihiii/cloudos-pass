<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tanli.cloud.dao.K8sRcDao">
    <insert id="addRc" parameterType="com.tanli.cloud.model.K8s_Rc">
        INSERT INTO tl_rc (uuid, deployment_uuid, name, namespace, replicas, selector, template, create_time, update_time)
        VALUES (#{uuid}, #{deployment_uuid}, #{name}, #{namespace}, #{replicas}, #{selector}, #{template}, #{create_time}, #{update_time})
    </insert>
</mapper>